# Compiler-for-end-to-end-ML-pipelines
A compiler that takes a simple ML pipeline script and turns it into an optimized, runnable program. It has all the classic stages of a real compiler, just applied to ML pipeline code instead of general-purpose code.
