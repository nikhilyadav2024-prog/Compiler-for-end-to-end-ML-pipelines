pipeline churn_prediction {
    load data "customer.csv";

    preprocess {
        fill_missing age;
        scale income;
        encode city;
    }

    split train 80 test 20;

    model random_forest {
        trees = 100;
    }

    train;
    evaluate metrics [accuracy, f1];
}
